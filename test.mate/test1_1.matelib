@``let `varname ``= `varvalue @$auto `varname = `varvalue$@
@``let `varname ``as `vartype ``= `varvalue @$`vartype `varname = `varvalue$@

@`ptrtypeArr ``[ ``] ``♂ `ptrname @$::std::unique_ptr<`ptrtypeArr[]> `ptrname$@
@`ptrtype ``♂ `ptrname @$::std::unique_ptr<`ptrtype> `ptrname$@

@``using `name ``{ `context ``} @$do{
    `context
        `name.close();
    }while(0)$@
